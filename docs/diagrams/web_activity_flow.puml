@startuml
title Web 后台操作活动流程图

start
:登录 Web 后台;

if (是否已有单位?) then (是)
else (否)
  :创建单位;
endif

if (是否已有整理项目?) then (是)
else (否)
  :创建整理项目;
endif

:进入上传页;
:选择整理项目;
:上传图片或 zip;
:查看上传记录;
:点击开始处理;
:进入在线跑批进度页;

repeat
  :查看任务状态和事件;
repeat while (批次仍在运行?) is (是)

if (批次处理成功?) then (是)
  :查看档案结果列表;
  :打开档案详情;
  if (发现核心字段错误?) then (是)
    :进入人工修正表单;
    :保存修正;
    :查看修订记录;
    :查看审计记录;
  else (否)
    :确认结果;
  endif
  :查看 JSON/CSV 导出;
else (否)
  :查看失败原因;
  :准备重试或使用兜底结果;
endif

stop

@enduml

